# Server
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# Security
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Storage
DB_PATH=./data/xeere.db

# CORS (comma separated origins, or * for all)
CORS_ORIGIN=*
